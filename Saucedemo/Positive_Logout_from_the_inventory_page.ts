import { test, expect } from '@playwright/test';

test("Positive: Logout from the inventory page", async ({ page }) => {
  // Step 1: Navigate to https://www.saucedemo.com/
  // Step 2: Fill the email field with 'standard_user'
  // Step 3: Fill the password field with 'secret_sauce'
  // Step 4: Click the login button
  // Step 5: Click the 'Open Menu' button on the inventory page
  // Step 6: Select 'Logout' from the menu options
  // Step 7: Verify the user is redirected to the login page at https://www.saucedemo.com/ with login form visible
});

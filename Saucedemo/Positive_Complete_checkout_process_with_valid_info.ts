import { test, expect } from '@playwright/test';

test("Positive: Complete checkout process with valid info", async ({ page }) => {
  // Step 1: Navigate to https://www.saucedemo.com/
  // Step 2: Fill the email field with 'standard_user'
  // Step 3: Fill the password field with 'secret_sauce'
  // Step 4: Click the login button
  // Step 5: Click the cart icon
  // Step 6: Click the Checkout button
  // Step 7: Fill the First Name field with 'John'
  // Step 8: Fill the Last Name field with 'Doe'
  // Step 9: Fill the Postal Code field with '12345'
  // Step 10: Click the Continue button
  // Step 11: Click the Finish button
});

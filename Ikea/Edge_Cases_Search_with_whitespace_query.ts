import { test, expect } from '@playwright/test';

test("Edge Cases: Search with whitespace query", async ({ page }) => {
  // Step 1: Navigate to https://www.ikea.com/us/en/
  // Step 2: Fill the search field with '   '
  // Step 3: Submit the search
});

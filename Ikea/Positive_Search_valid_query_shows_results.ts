import { test, expect } from '@playwright/test';

test("Positive: Search valid query shows results", async ({ page }) => {
  // Step 1: Navigate to https://www.ikea.com/us/en/
  // Step 2: Enter 'test' into the 'What are you looking for?' search field
  // Step 3: Click the 'Search' button
});

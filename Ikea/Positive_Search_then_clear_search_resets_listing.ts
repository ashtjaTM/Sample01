import { test, expect } from '@playwright/test';

test("Positive: Search then clear search resets listing", async ({ page }) => {
  // Step 1: Navigate to https://www.ikea.com/us/en/
  // Step 2: Fill the search field with 'test'
  // Step 3: Submit the search
  // Step 4: Click the 'Clear the search input field' button
});

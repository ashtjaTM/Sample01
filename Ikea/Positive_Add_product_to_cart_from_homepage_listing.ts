import { test, expect } from '@playwright/test';

test("Positive: Add product to cart from homepage listing", async ({ page }) => {
  // Step 1: Navigate to https://www.ikea.com/us/en/
  // Step 2: Click the first product card on the homepage.
  // Step 3: Click 'Add "GÖRSNYGG Storage case" to cart' button on the product card.
});

import { test, expect } from '@playwright/test';

test("Edge Cases: Add multiple different products to cart", async ({ page }) => {
  // Step 1: Navigate to https://www.ikea.com/us/en/
  // Step 2: Add 'GÖRSNYGG Storage case' to cart
  // Step 3: Add 'KALLAX Shelf unit' to cart
});

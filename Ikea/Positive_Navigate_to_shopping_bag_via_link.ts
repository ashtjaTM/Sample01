import { test, expect } from '@playwright/test';

test("Positive: Navigate to shopping bag via link", async ({ page }) => {
  // Step 1: Navigate to URL https://www.ikea.com/us/en/
  // Step 2: Click the 'Shopping bag' link in the header navigation
});

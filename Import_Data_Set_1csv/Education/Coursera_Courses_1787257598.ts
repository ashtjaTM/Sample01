import { test, expect } from '@playwright/test';

test("Coursera Courses_1787257598", async ({ page }) => {
  // Step 1: Navigate to https://www.coursera.org/
  // Step 2: Click the Explore menu
  // Step 3: Select Project Management from the menu
});

import { test, expect } from '@playwright/test';

test("National Geographic_1787257598", async ({ page }) => {
  // Step 1: Navigate to https://www.nationalgeographic.com/
  // Step 2: Click the 'Science' option
  // Step 3: Verify that the science section opens
});
